# Generated by IaCTranslate. Adjust values before applying.
region       = "{{ region }}"
project_name = "{{ project_slug }}"
do_token     = "REPLACE_ME_DO_TOKEN"

# ssh_key_fingerprints: list the fingerprints of SSH keys already uploaded to
# your DigitalOcean account (Settings -> Security -> SSH Keys), e.g.:
# ssh_key_fingerprints = ["aa:bb:cc:...:zz"]
