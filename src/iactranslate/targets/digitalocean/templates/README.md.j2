# {{ project }}

Production-ready Terraform (digitalocean) generated by **IaCTranslate** for a {{ plan.source_platform }} → DigitalOcean migration.

- **Source platform:** {{ plan.source_platform }}
- **Target:** {{ plan.target }} ({{ region }})
- **Droplets:** {{ compute | length }}
- **Estimated cost:** ${{ cost_total_display }}/month (on-demand, approximate)

{% set windows_vms = [] %}
{% for c in compute %}{% if c.os_family_changed %}{% set _ = windows_vms.append(c) %}{% endif %}{% endfor %}
{% if windows_vms %}
## ⚠ Windows source VMs — read this first

DigitalOcean has **no Windows Server image** in its catalog — Droplets are Linux-only
unless you bring your own custom image. The following source VM(s) have been rendered as
**Ubuntu 22.04** Droplets so the plan is still valid, but this is very likely not what you
want:

{% for c in windows_vms %}
- `{{ c.vm_name }}` (source OS: `{{ c.source_os }}`, rendered as `{{ c.image_key }}`)
{% endfor %}

If these workloads genuinely need Windows Server, DigitalOcean is not a fit for them —
choose AWS, Azure, GCP, or OCI instead, or bring your own Windows custom image to DO
(`digitalocean_custom_image`) and edit `compute.tf` to reference it.

{% endif %}
## Before you apply

1. **Set your API token.** Fill in `do_token` in `terraform.tfvars` (or `TF_VAR_do_token`) —
   generate one at API → Tokens in the DO control panel.
2. **Upload an SSH key first.** DO Droplets are provisioned with SSH keys already known to
   your account, not an inline public key — upload yours (Settings → Security → SSH Keys),
   then list its fingerprint in `ssh_key_fingerprints`.
3. **Images resolve automatically** to public image slugs (e.g. `ubuntu-22-04-x64`) in
   `compute.tf` — nothing to fill in.
4. **Review sizing.** Droplet sizes were selected to fit each source VM's vCPU and memory
   with headroom (`m-` memory-optimized for database/cache). Root disk size is bundled into
   the Droplet size and isn't independently configurable — see `storage.tf` for extra volumes.
5. **Review networking.** A VPC (`{{ network.vpc_cidr }}`) with tag-based firewalls is
   defined. DigitalOcean has no subnet or managed NAT gateway resources — every Droplet joins
   the VPC directly. Every Droplet gets a public IPv4 by default (DO has no flag to suppress
   it); "private" tier isolation comes from firewall rules, not from lacking a public IP.

## Deploy

```bash
terraform init
terraform plan
terraform apply
```

## What was migrated

| Source VM | Droplet size | Tier | Environment | Est. $/mo |
|-----------|--------------|------|-------------|----------:|
{% for c in compute %}
| {{ c.vm_name }} | {{ c.instance_type }} | {{ c.tier.value }} | {{ c.environment.value }} | {{ "%.2f"|format(c.estimated_monthly_cost_usd) }} |
{% endfor %}

_Generated deterministically from your discovery export. AI was used only for
classification and sizing decisions; all Terraform was rendered from templates._
