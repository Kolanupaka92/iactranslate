# {{ project }}
#
# Generated by IaCTranslate — {{ plan.source_platform }} -> DigitalOcean migration.
# Source platform: {{ plan.source_platform }}   Target: {{ plan.target }}   Region: {{ region }}
# {{ compute | length }} Droplet(s), estimated ${{ cost_total_display }}/month on-demand.
#
# Resource layout:
#   versions.tf     - Terraform + digitalocean provider version constraints
#   provider.tf     - digitalocean provider configuration (API token)
#   variables.tf    - input variables (token, SSH keys)
#   terraform.tfvars- variable values (edit before apply)
#   networking.tf   - VPC (DO has no subnet/NAT gateway resources)
#   security.tf     - tags + firewalls
#   loadbalancer.tf - Load Balancers fronting multi-instance tiers
#   compute*.tf      - Droplets (one per source VM)
#   storage.tf      - additional block storage volumes
#   outputs.tf      - VPC / Droplet outputs
#
# This file intentionally contains no resources; see the files above.
