# {{ project }}
#
# Generated by IaCTranslate — VMware -> GCP migration.
# Source platform: {{ plan.source_platform }}   Target: {{ plan.target }}   Region: {{ region }}
# {{ compute | length }} instance(s), estimated ${{ "%.2f"|format(plan.total_estimated_monthly_cost_usd) }}/month on-demand.
#
# Resource layout:
#   versions.tf     - Terraform + google provider version constraints
#   provider.tf     - google provider configuration (project/region/zone)
#   variables.tf    - input variables (project, region, zone, image map)
#   terraform.tfvars- variable values (edit before apply)
#   networking.tf   - VPC network, subnetworks, Cloud NAT
#   security.tf     - firewall rules (scoped by network tag)
#   compute*.tf      - Compute Engine instances (one per source VM)
#   storage.tf      - additional persistent data disks
#   outputs.tf      - network / instance outputs
#
# This file intentionally contains no resources; see the files above.
