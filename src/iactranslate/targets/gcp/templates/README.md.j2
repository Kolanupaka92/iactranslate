# {{ project }}

Production-ready Terraform (google) generated by **IaCTranslate** for a VMware → GCP migration.

- **Source platform:** {{ plan.source_platform }}
- **Target:** {{ plan.target }} ({{ region }})
- **Instances:** {{ compute | length }}
- **Estimated cost:** ${{ cost_total_display }}/month (on-demand, approximate)

## Before you apply

1. **Set your project.** Fill in `gcp_project` in `terraform.tfvars` (or via `TF_VAR_gcp_project`).
2. **Boot images resolve automatically** to public image families (e.g.
   `ubuntu-os-cloud/ubuntu-2204-lts`) in `compute.tf` — nothing to fill in.
3. **Review sizing.** Machine types were selected to fit each source VM's vCPU and memory with
   headroom (n2-highmem for database/cache). Adjust in `compute.tf` if needed.
4. **Review networking.** A VPC (`{{ network.vpc_cidr }}`) with subnetworks, tag-scoped firewall
   rules, external IPs for the web tier, and {{ "Cloud NAT for private egress" if network.nat_gateway else "no NAT" }} is defined.

## Deploy

```bash
gcloud auth application-default login
terraform init
terraform plan
terraform apply
```

## What was migrated

| Source VM | Machine type | Tier | Environment | Boot disk (GiB) | Est. $/mo |
|-----------|--------------|------|-------------|----------------:|----------:|
{% for c in compute %}
| {{ c.vm_name }} | {{ c.instance_type }} | {{ c.tier.value }} | {{ c.environment.value }} | {{ c.root_volume_gib }} | {{ "%.2f"|format(c.estimated_monthly_cost_usd) }} |
{% endfor %}

_Generated deterministically from your discovery export. AI was used only for
classification and sizing decisions; all Terraform was rendered from templates._
