# Generated by IaCTranslate. Adjust values before applying.
gcp_project  = "REPLACE_ME_PROJECT_ID"
region       = "{{ region }}"
zone         = "{{ region }}-a"
project_name = "{{ project_slug }}"

# Fill in real boot image references before `terraform apply`.
image_ids = {
{% for key in image_keys %}
  "{{ key }}" = "REPLACE_ME"
{% endfor %}
}
