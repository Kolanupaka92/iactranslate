# Generated by IaCTranslate. Adjust values before applying.
gcp_project  = "REPLACE_ME_PROJECT_ID"
region       = "{{ region }}"
zone         = "{{ region }}-a"
project_name = "{{ project_slug }}"

# OS boot images are resolved automatically to public image families (see compute.tf).
# Only gcp_project above needs your real value before `terraform apply`.
