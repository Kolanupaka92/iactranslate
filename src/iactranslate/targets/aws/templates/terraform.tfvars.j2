# Generated by IaCTranslate. Adjust values before applying.
region       = "{{ region }}"
project_name = "{{ project }}"

# AMIs are resolved automatically (see images.tf). Uncomment to pin specific images:
# ami_overrides = {
{% for key in image_keys %}
#   "{{ key }}" = "ami-0123456789abcdef0"
{% endfor %}
# }
