# Generated by IaCTranslate. Adjust values before applying.
region       = "{{ region }}"
project_name = "{{ project }}"

# Fill in real AMI IDs for your account/region before `terraform apply`.
ami_ids = {
{% for key in image_keys %}
  "{{ key }}" = "ami-REPLACE_ME"
{% endfor %}
}
