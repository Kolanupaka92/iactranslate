# {{ project }}
#
# Generated by IaCTranslate — VMware -> AWS migration.
# Source platform: {{ plan.source_platform }}   Target: {{ plan.target }}   Region: {{ region }}
# {{ compute | length }} instance(s), estimated ${{ cost_total_display }}/month on-demand.
#
# Resource layout:
#   versions.tf     - Terraform + provider version constraints
#   provider.tf     - AWS provider configuration
#   variables.tf    - input variables (region, AMI map)
#   terraform.tfvars- variable values (edit before apply)
#   networking.tf   - VPC, subnets, gateways, routing
#   security.tf     - security groups
#   compute*.tf      - EC2 instances (one per source VM)
#   storage.tf      - additional data volumes
#   outputs.tf      - VPC / instance outputs
#
# This file intentionally contains no resources; see the files above.
