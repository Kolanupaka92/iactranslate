# {{ project }}

Production-ready Terraform (azurerm) generated by **IaCTranslate** for a VMware → Azure migration.

- **Source platform:** {{ plan.source_platform }}
- **Target:** {{ plan.target }} ({{ region }})
- **VMs:** {{ compute | length }}
- **Estimated cost:** ${{ cost_total_display }}/month (pay-as-you-go, approximate)

## Terraform state

{% if state_backend.is_local %}
> **State is local.** No backend is configured, so Terraform writes
> `terraform.tfstate` beside these files. That file *is* the mapping between this
> configuration and your real infrastructure — it cannot be shared with a
> teammate, it is not locked against two people applying at once, and if this
> machine is lost the estate becomes unmanageable by Terraform.
>
> Configure a remote backend before you apply anything you intend to keep.
> Re-generate with `--state-backend auto` to have one written for you, or add a
> `backend` block to `versions.tf` by hand and run `terraform init -migrate-state`.
{% elif state_backend.is_complete %}
State is stored remotely via the `{{ state_backend.kind }}` backend configured in
`versions.tf`. Run `terraform init` to initialise it.

Make sure the backing bucket/container has **versioning enabled** — it is the only
thing standing between a bad apply and an unrecoverable estate — and that state
locking is on, so two concurrent applies cannot corrupt it.
{% else %}
The `{{ state_backend.kind }}` backend is declared in `versions.tf` but left
**partially configured** on purpose: this tool does not know your bucket or
account names.

```bash
cp backend.hcl.example backend.hcl   # fill in the blanks
terraform init -backend-config=backend.hcl
```

`terraform init` fails until you do this, which is deliberate — the alternative
is silently falling back to local state.
{% endif %}

## Before you apply

1. **Images resolve automatically.** Each VM uses a `source_image_reference` to the
   right marketplace image (publisher/offer/sku, `version = "latest"`) — nothing to fill in.
2. **Set admin credentials.** `admin_ssh_public_key` (Linux VMs) and `admin_password`
   (Windows VMs) — supply securely (e.g. `TF_VAR_admin_password`), never commit secrets.
3. **Review sizing.** VM sizes were selected to fit each source VM's vCPU and memory with
   headroom (E-series for database/cache). Adjust in `compute.tf` if needed.
4. **Review networking.** A VNet (`{{ network.vpc_cidr }}`) with public/private subnets,
   per-tier NSGs, and public IPs for the web tier is defined.

## Deploy

```bash
az login
terraform init
terraform plan
terraform apply
```

## What was migrated

| Source VM | VM size | Tier | Environment | OS disk (GiB) | Est. $/mo |
|-----------|---------|------|-------------|--------------:|----------:|
{% for c in compute %}
| {{ c.vm_name }} | {{ c.instance_type }} | {{ c.tier.value }} | {{ c.environment.value }} | {{ c.root_volume_gib }} | {{ "%.2f"|format(c.estimated_monthly_cost_usd) }} |
{% endfor %}

_Generated deterministically from your discovery export. AI was used only for
classification and sizing decisions; all Terraform was rendered from templates._
