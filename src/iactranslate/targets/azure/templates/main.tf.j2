# {{ project }}
#
# Generated by IaCTranslate — VMware -> Azure migration.
# Source platform: {{ plan.source_platform }}   Target: {{ plan.target }}   Location: {{ region }}
# {{ compute | length }} VM(s), estimated ${{ cost_total_display }}/month pay-as-you-go.
#
# Resource layout:
#   versions.tf     - Terraform + azurerm provider version constraints
#   provider.tf     - azurerm provider configuration
#   variables.tf    - input variables (location, RG, image map, admin creds)
#   terraform.tfvars- variable values (edit before apply)
#   networking.tf   - resource group, VNet, subnets
#   security.tf     - network security groups
#   compute*.tf      - public IPs, NICs, and VMs (one per source VM)
#   storage.tf      - additional managed data disks
#   outputs.tf      - RG / VNet / VM outputs
#
# This file intentionally contains no resources; see the files above.
