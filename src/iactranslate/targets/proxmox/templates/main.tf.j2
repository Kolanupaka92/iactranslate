# {{ project }}
#
# Generated by IaCTranslate — {{ plan.source_platform }} -> Proxmox VE migration.
# Source platform: {{ plan.source_platform }}   Target: {{ plan.target }}   Node: {{ region }}
# {{ compute | length }} VM(s). Cost is not estimated for an on-premises target — see README.md.
#
# Resource layout:
#   versions.tf     - Terraform + bpg/proxmox provider version constraints
#   provider.tf     - Proxmox API connection (token, not password)
#   variables.tf    - inputs (endpoint, node, datastore, bridge, VLAN ids, template ids)
#   terraform.tfvars- variable values (edit before apply)
#   networking.tf   - VLAN layout (bridges are node-owned and not created)
#   security.tf     - firewall security groups, one per tier
#   loadbalancer.tf - load-balancer requirement (not generated on Proxmox)
#   compute*.tf      - VMs cloned from templates, data disks inline, firewall attached
#   outputs.tf      - VM ids and assigned addresses
#
# This file intentionally contains no resources; see the files above.
