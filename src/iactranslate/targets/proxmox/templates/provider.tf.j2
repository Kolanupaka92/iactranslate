# Generated by IaCTranslate.
#
# `bpg/proxmox` rather than the older `Telmate/proxmox`: it is the actively
# maintained provider, models the firewall's security groups (which this plan
# needs), and supports API tokens so no root password sits in a tfvars file.
provider "proxmox" {
  endpoint  = var.proxmox_endpoint
  api_token = var.proxmox_api_token
  insecure  = var.proxmox_insecure
}
