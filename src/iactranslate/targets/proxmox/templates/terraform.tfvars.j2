# Generated by IaCTranslate. Adjust values before applying.
project_name      = "{{ project_slug }}"
proxmox_endpoint  = "REPLACE_ME_PROXMOX_ENDPOINT"
proxmox_api_token = "REPLACE_ME_API_TOKEN"
node_name         = "{{ region }}"
datastore_id      = "local-lvm"
bridge            = "vmbr0"

# VLAN tags — assign the ones your network team allocated:
{% for subnet in network.subnets %}
# vlan_id_{{ subnet.resource_name }} = {{ 100 + loop.index0 }}   # {{ subnet.name }} {{ subnet.cidr }}
{% endfor %}

# Template VM ids to clone:
{% for key in image_keys %}
# {{ image_refs[key].variable }} = 9000
{% endfor %}
