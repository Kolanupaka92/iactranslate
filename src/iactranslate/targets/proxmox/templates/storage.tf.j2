# Generated by IaCTranslate — additional disks.
#
# On Proxmox a data disk is a `disk` block on the VM, not a separate resource;
# extra volumes are emitted inline in compute.tf. This file exists so the
# layout matches every other target.
{% if not volumes %}
# No additional disks — every source VM fits on its root disk.
{% endif %}
