# Generated by IaCTranslate — network layout on Proxmox VE.
#
# Proxmox has no subnet resource. VMs attach to a Linux bridge (`var.bridge`,
# usually vmbr0) and are isolated by VLAN tag on the network device — see
# compute.tf. The bridge itself normally already exists on the node and is
# owned by the node's /etc/network/interfaces, so it is not created here;
# creating it from Terraform is possible (proxmox_virtual_environment_network_
# linux_bridge) but replaces node-level network config and is not something a
# migration tool should do unasked.
#
# The plan's address space {{ network.vpc_cidr }} carves into these VLANs:
{% for subnet in network.subnets %}
#   {{ subnet.name }}  {{ subnet.cidr }}  ({{ subnet.tier.value }})  -> vlan var.vlan_id_{{ subnet.resource_name }}
{% endfor %}
#
# Addresses are assigned by cloud-init at clone time (compute.tf) from those
# ranges: .10 upward within each /24, gateway at .1. Adjust if your network
# uses a different convention.
{% if network.nat_gateway %}
#
# The plan places workloads in private VLANs that need controlled egress.
# On Proxmox that is a routing/NAT decision at your edge (or a firewall VM),
# not a resource this provider creates — recorded here so it is not lost.
{% endif %}
