# Generated by IaCTranslate. Adjust values before applying.
project_name           = "{{ project_slug }}"
prism_central_endpoint = "REPLACE_ME_PRISM_CENTRAL"
nutanix_username       = "REPLACE_ME_USERNAME"
nutanix_password       = "REPLACE_ME_PASSWORD"
cluster_name           = "{{ region }}"

# VLAN ids — assign the ones your network team allocated:
{% for subnet in network.subnets %}
# vlan_id_{{ subnet.resource_name }} = {{ 100 + loop.index0 }}   # {{ subnet.name }} {{ subnet.cidr }}
{% endfor %}

# Image names as they appear in Prism Central:
{% for key in image_keys %}
# {{ image_refs[key].variable }} = "REPLACE_ME"
{% endfor %}
