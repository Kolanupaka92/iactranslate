{% if hybrid and hybrid.required %}
# Generated by IaCTranslate — connectivity to networks that are not migrating.
#
# Observed flows show this estate depends on:
{% for net in hybrid.networks %}
#   {{ net.cidr }} — {{ net.connections }} connections from {{ net.dependent_workloads | length }} workload(s), ports {{ net.ports | join(", ") }}{% if net.overlaps_vpc %}  [OVERLAPS THE PLANNED RANGE]{% endif %}

{% endfor %}
{% for note in hybrid.notes %}
#
# {{ note }}
{% endfor %}
#
# This is an on-premises target, so these networks are most likely already
# reachable over your existing LAN — the relevant question is whether the
# planned ranges above route to them, not whether a tunnel exists. Nothing is
# generated; the dependency is recorded so it is checked, not assumed.
{% endif %}
