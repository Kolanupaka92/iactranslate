# Generated by IaCTranslate — additional disks.
#
# On AHV a data disk is part of the VM's disk_list, not a separate resource;
# extra volumes are therefore emitted inline in compute.tf. This file exists
# so the layout matches every other target.
{% if not volumes %}
# No additional disks — every source VM fits on its root disk.
{% endif %}
