# {{ project }}
#
# Generated by IaCTranslate — {{ plan.source_platform }} -> Nutanix AHV migration.
# Source platform: {{ plan.source_platform }}   Target: {{ plan.target }}   Cluster: {{ region }}
# {{ compute | length }} VM(s). Cost is not estimated for an on-premises target — see README.md.
#
# Resource layout:
#   versions.tf     - Terraform + nutanix provider version constraints
#   provider.tf     - Prism Central connection + target cluster lookup
#   variables.tf    - inputs (credentials, cluster, VLAN ids, image names)
#   terraform.tfvars- variable values (edit before apply)
#   networking.tf   - VLAN-backed subnets
#   security.tf     - tier categories + the firewall intent per tier
#   loadbalancer.tf - load-balancer requirement (not generated on AHV)
#   compute*.tf      - VMs (one per source VM), data disks inline
#   outputs.tf      - cluster / VM / subnet outputs
#
# This file intentionally contains no resources; see the files above.
