# Generated by IaCTranslate. Adjust values before applying.
region         = "{{ region }}"
project_name   = "{{ project_slug }}"
compartment_id = "REPLACE_ME_COMPARTMENT_OCID"

# Auth: tenancy_ocid, user_ocid, fingerprint, private_key_path — see README.md
# for how to generate an API signing key. Prefer environment variables
# (TF_VAR_tenancy_ocid, etc.) or a secrets manager over committing these.
