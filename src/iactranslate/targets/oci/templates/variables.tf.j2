# Generated by IaCTranslate.
variable "region" {
  description = "OCI region to deploy into."
  type        = string
  default     = "{{ region }}"
}

variable "project_name" {
  description = "Project name used as a resource name prefix and freeform tag."
  type        = string
  default     = "{{ project_slug }}"
}

variable "compartment_id" {
  description = "OCID of the compartment to create resources in. Set before `terraform apply`."
  type        = string
  default     = "REPLACE_ME_COMPARTMENT_OCID"
}

variable "tenancy_ocid" {
  description = "OCID of your OCI tenancy."
  type        = string
  default     = "REPLACE_ME_TENANCY_OCID"
}

variable "user_ocid" {
  description = "OCID of the OCI user making API calls."
  type        = string
  default     = "REPLACE_ME_USER_OCID"
}

variable "fingerprint" {
  description = "Fingerprint of the API signing key uploaded for this user."
  type        = string
  default     = "REPLACE_ME_KEY_FINGERPRINT"
}

variable "private_key_path" {
  description = "Path to the API signing key's private key (PEM)."
  type        = string
  default     = "~/.oci/oci_api_key.pem"
}

variable "admin_ssh_public_key" {
  description = "SSH public key for Linux instances. Set before `terraform apply`."
  type        = string
  default     = "ssh-rsa REPLACE_ME"
}

# Images are resolved automatically via data "oci_core_images" (see images.tf) —
# no image OCIDs to fill in. Windows instances get an auto-generated initial
# password retrievable after launch via `oci compute instance get-windows-initial-credentials`.
