# {{ project }}
#
# Generated by IaCTranslate — {{ plan.source_platform }} -> OCI migration.
# Source platform: {{ plan.source_platform }}   Target: {{ plan.target }}   Region: {{ region }}
# {{ compute | length }} instance(s), estimated ${{ cost_total_display }}/month on-demand.
#
# Resource layout:
#   versions.tf     - Terraform + oci provider version constraints
#   provider.tf     - oci provider configuration (API key auth) + AD lookup
#   variables.tf    - input variables (compartment, tenancy, auth, admin key)
#   terraform.tfvars- variable values (edit before apply)
#   networking.tf   - VCN, subnets, internet/NAT gateways, routing
#   security.tf     - Network Security Groups
#   images.tf       - platform image lookups (resolved at apply time)
#   loadbalancer.tf - flexible Load Balancers fronting multi-instance tiers
#   compute*.tf      - Compute instances (one per source VM)
#   storage.tf      - additional block volumes
#   outputs.tf      - VCN / subnet / instance outputs
#
# This file intentionally contains no resources; see the files above.
