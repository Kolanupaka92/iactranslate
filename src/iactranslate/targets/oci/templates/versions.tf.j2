# Generated by IaCTranslate — do not edit by hand.
{% if state_backend.is_local %}
#
# ⚠ STATE IS LOCAL. Terraform will write terraform.tfstate next to this file.
#   That cannot be shared with a team, cannot be locked against concurrent
#   applies, and loses the mapping between this config and real infrastructure
#   if this machine does. Configure a remote backend before applying anything
#   you intend to keep — see README.md, "Terraform state".
{% endif %}
terraform {
  required_version = ">= 1.5.0"
{% if state_backend_block %}
{{ state_backend_block }}
{% endif %}

  required_providers {
    oci = {
      source  = "oracle/oci"
      version = "~> 5.0"
    }
  }
}
